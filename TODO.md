# TODO

- add a visual indicator to the status bar when the extension is enabled/disabled (maybe color it red when disabled?)
- add a visual indicator to the status bar when the extension 'stopped' a key (i.e. like a visual bell; this may require a new function other than noop)
- add option to change preferences of workbench elements like cursor, window borders, etc. see: https://code.visualstudio.com/api/references/theme-color (and my settings.json)